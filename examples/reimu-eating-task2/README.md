# Reimu Eating Task 2 example

The six PNGs in `frames/` are programmatically drawn placeholders for exercising
the harness. They are not final Reimu artwork and must not be represented as a
completed animation.

Conceptual sequence: hold onigiri, raise it, bite, puff cheeks, chew, and return.
Regenerate placeholders with `scripts/create_placeholder_example.py`.

